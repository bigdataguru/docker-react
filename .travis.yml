sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t bigdataguru/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true bigdataguru/docker-react npm run test
